<div class="container-fluid p-0" style="height: calc(100vh - 55px);">
  <div class="row g-0 h-100">
    <!-- Sidebar -->
    <div class="col-md-4 col-lg-3 bg-white border-end d-md-block collapse" id="sidebar">
      <!-- Header del sidebar -->
      <div class="d-flex align-items-center justify-content-between p-3 border-bottom bg-secondary">
        <div class="d-flex align-items-center">
          <img src="https://placehold.co/40" alt="Mi perfil" class="rounded-circle me-3">
          <h6 class="mb-0">Historial</h6>
          <br/>
        </div>

      </div>
      <!-- Lista de contactos/conversaciones -->
      <div class="overflow-auto" style="height: calc(100vh - 140px);">
        <div class="list-group list-group-flush">
          <% @phones.each do |phone| %>
            <a href="#" class="list-group-item list-group-item-action <%= 'bg-primary-subtle' if phone == @chat_session_id %> border-0 p-3">
              <div class="d-flex align-items-center">
                <img src="https://placehold.co/50" alt="Contacto" class="rounded-circle me-3">
                <div class="flex-grow-1">
                  <div class="d-flex justify-content-between align-items-start">
                    <h6 class="mb-1"><%= phone %></h6>
                    <!-- <small class="text-muted">10:30</small> -->
                  </div>
                  <!-- <p class="mb-0 text-muted small">
                    Hola, ¿cómo estás?
                  </p> -->
                </div>
                <!-- <span class="badge bg-success rounded-pill">2</span> -->
              </div>
            </a>
          <%end%>
        </div>
      </div>
    </div>

    <!-- Área principal del chat -->
    <div class="col-md-8 col-lg-9 d-flex flex-column h-100 ">
      <!-- Header del chat -->
      <div class="d-flex align-items-center justify-content-between p-3 border-bottom bg-secondary">
        <div class="d-flex align-items-center">
          <button class="btn btn-outline-secondary btn-sm me-3 d-md-none" type="button" data-bs-toggle="collapse"
            data-bs-target="#sidebar">
            <i class="bi bi-list"></i>
          </button>
          <img src="https://placehold.co/40" alt="Contacto" class="rounded-circle me-3">
          <div>
            <h6 class="mb-0">
              <%= @chat_session_id %>
            </h6>
            <small class="text-muted">En línea</small>
          </div>
        </div>
        <!-- <div class="dropdown">
          <button class="btn btn-outline-secondary btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
            <i class="bi bi-three-dots-vertical"></i>
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#"><i class="bi bi-info-circle me-2"></i>Info del contacto</a></li>
            <li><a class="dropdown-item" href="#"><i class="bi bi-search me-2"></i>Buscar mensajes</a></li>
            <li>
              <hr class="dropdown-divider">
            </li>
            <li><a class="dropdown-item text-danger" href="#"><i class="bi bi-trash me-2"></i>Eliminar chat</a></li>
          </ul>
        </div> -->
      </div>

      <!-- Mensajes del chat -->
      <div class="flex-grow-1 overflow-auto p-3 bg-light">
        <% @conversation.each do |chat| %>
          <% if chat.message["type"] == "human" %>
            <!-- Mensaje recibido -->
            <div class="d-flex mb-3">
              <div class="me-2">
                <img src="https://placehold.co/30" alt="Usuario" class="rounded-circle">
              </div>
              <div class="bg-white p-3 rounded shadow-sm" style="max-width: 70%;">
                <p class="mb-1">
                  <%= whatsapp_message_to_html(chat.message["content"]) %>
                </p>
                <small class="text-muted">
                  <%= chat.created_at.to_formatted_s(:long_ordinal) %>
                </small>
              </div>
            </div>
          <% else %>
            <!-- Mensaje enviado -->
            <div class="d-flex justify-content-end mb-3">
              <div class="bg-secondary bg-gradient text-white p-3 rounded shadow-sm" style="max-width: 70%;">
                <p class="mb-1">
                  <%= whatsapp_message_to_html(chat.message["content"]) %>
                </p>
                <div class="d-flex justify-content-end align-items-center">
                  <small class="me-2">
                    <%= chat.created_at.to_formatted_s(:long_ordinal) %>
                  </small>
                  <%= svg("bi-check-all", width: "16", height: "16", fill: "#98FB98") %>
                </div>
              </div>
            </div>
          <% end %>
        <% end %>


        <!-- Mensaje recibido -->
        <!-- <div class="d-flex mb-3">
          <div class="me-2">
            <img src="https://placehold.co/30" alt="Usuario" class="rounded-circle">
          </div>
          <div class="bg-white p-3 rounded shadow-sm" style="max-width: 70%;">
            <p class="mb-1">Muy bien también. ¿Nos vemos esta tarde para revisar el proyecto?</p>
            <small class="text-muted">10:35</small>
          </div>
        </div> -->

        <!-- Mensaje enviado -->
        <!-- <div class="d-flex justify-content-end mb-3">
          <div class="bg-primary text-white p-3 rounded shadow-sm" style="max-width: 70%;">
            <p class="mb-1">¡Perfecto! Nos vemos a las 4 PM en la oficina.</p>
            <div class="d-flex justify-content-end align-items-center">
              <small class="me-2">10:37</small>
              <i class="bi bi-check-all text-info"></i>
            </div>
          </div>
        </div> -->

        <!-- Mensaje con imagen -->
        <!-- <div class="d-flex mb-3">
          <div class="me-2">
            <img src="https://placehold.co/30" alt="Usuario" class="rounded-circle">
          </div>
          <div class="bg-white p-3 rounded shadow-sm" style="max-width: 70%;">
            <img src="https://placehold.co/200x150" alt="Imagen" class="img-fluid rounded mb-2">
            <p class="mb-1">Mira esta imagen del proyecto</p>
            <small class="text-muted">10:40</small>
          </div>
        </div>
      </div> -->

      <!-- Input para escribir mensajes -->
      <div class="d-flex justify-content-center mb-3">
        <div class="bg-success bg-gradient text-white p-3 rounded shadow-sm" style="max-width: 70%;">
          <p class="mb-1">
            Fin de la conversación
            <%= svg("bi-check-all", width: "16", height: "16", fill: "#98FB98") %>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>